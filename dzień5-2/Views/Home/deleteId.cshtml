@{
  var id = (int?)ViewData["Id"] ?? default!;
  var village = VillagesModel.Remove(id);
}

<div style="font-size: 1.5rem;" class="text-center">
Panie, wieś <b>@village.Name</b> została usunięta!
  <form action="/">
    <input type="submit" value="wróć">
  </form>
</div>